/**
 * Session restoration helpers — re-exported from @drakkar.software/octospaces-sdk.
 * `sessionFromPersisted`: rebuild a live Session from a persisted vault entry.
 * `activeAccountOf`: return the account matching activeId, else the first.
 */
export { sessionFromPersisted, activeAccountOf } from '@drakkar.software/octospaces-sdk';
